# Avatar Builder

Suggested future workers:

- 2.5D avatar prep
- TripoSR / InstantMesh for heavier reconstruction paths

Current state:

- `main.py` is a placeholder worker contract
- Later this can consume user uploads and emit a preview asset package
