# LLM Recommender

Suggested future worker:

- Qwen2.5-7B LoRA for recommendation text generation

Current state:

- `main.py` provides a FastAPI stub
- Keep the request/response contract stable while you replace the internals
