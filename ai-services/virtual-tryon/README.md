# Virtual Try-On

Suggested future worker:

- OOTDiffusion / VITON for try-on imagery

Current state:

- `main.py` is a placeholder FastAPI service
- Reuse the same request contract when the real worker is ready
