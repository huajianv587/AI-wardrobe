# Classifier

Suggested future workers:

- CLIP ViT-L/14 for category and tags
- Qwen-VL LoRA for richer multimodal attributes

Current state:

- `main.py` exposes a FastAPI stub for `/health` and `/infer`
