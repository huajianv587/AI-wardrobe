# Product Renderer

Stub FastAPI worker for product-shot generation and creative rendering.

Planned role:

- Generate clean product hero images and art-directed showcase shots
- Accept garment image + prompt blocks
- Later host ControlNet + SD or your own custom rendering stack behind `/infer`
