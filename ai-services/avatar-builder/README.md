# Avatar Builder

Suggested future workers:

- 2.5D avatar prep as the MVP default
- TripoSR / InstantMesh only as a later upgrade path when you really need heavier reconstruction

Current state:

- `main.py` is a placeholder worker contract
- Later this can consume user uploads and emit a lightweight avatar base package for 2.5D try-on flows
