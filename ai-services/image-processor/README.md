# Image Processor

Suggested future workers:

- BiRefNet / RMBG-2.0 for garment cutout
- Real-ESRGAN for upscaling
- ControlNet + SD for product hero shots

Current state:

- `main.py` exposes `/health` and `/infer`
- Replace the stub response with your inference pipeline later
